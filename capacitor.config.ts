import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-image-upload-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
